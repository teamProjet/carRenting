{% extends 'base.html.twig' %}

{% block title %}Hello CatalogController!{% endblock %}

{% block body %}
<div class="search">

    <input onkeyup="search_car()" name="search" id="searchbar" placeholder="Rechercher">

</div>

<div class="container_catalog">



    <div class="carContCat">

{% for car in cars %}

        <div class="carPic">

        <h3 class="h3_catalog">{{car.marque}} {{car.modele}}</h3>

                <img src="{{car.image}}" alt="image de la voiture">

        <a href="{{path('catalog_show', { 'id': car.id } )}}">  

        <button class="btnMoreCar">Voir plus</button>

        </a>

        </div>

{% endfor %}

    </div>





    {# {% for car in cars %}
    <div class="card card_catalog">
        <div class="card-body catalog_contentBx">
            <h3 class="card-title h3_catalog">{{car.marque}} {{car.modele}}</h3>
            <h2 class="card-text h2_catalog">{{car.tarifJournee}} €/j</h2>
            <a href="{{ path('catalog_show', { 'id': car.id }) }}" class="btn btn-primary catalog_loc">Voir plus</a>
        </div>
        <img src="{{car.image}}" style="width: 645px; height:200px;" alt="image de la voiture">
    </div>
    {% endfor %} #}
</div>
{% endblock %}

